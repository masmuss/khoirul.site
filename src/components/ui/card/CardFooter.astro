---
import { cn } from "@/lib/utils";

interface Props {
	class?: string;
}

const { class: className }: Props = Astro.props;
---

<div class={cn("flex items-center rounded-b-xl px-4 group-data-[size=sm]/card:p-3", className)}>
	<slot />
</div>
